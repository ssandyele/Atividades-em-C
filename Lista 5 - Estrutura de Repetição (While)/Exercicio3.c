#include <stdio.h>
#include <stdlib.h>

/*Escreva um Algoritmo que imprima todos os números inteiros de 100 a 1 (em ordem
decrescente).*/

int main () {
    int i = 100;
    while (i>=1){
        printf("%d ", i);
        i--;
    }
    return 0;
}
